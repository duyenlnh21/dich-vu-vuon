import React from "react";
import { Text } from "native-base";
import Container from "./Container";

export default function Contact({ navigation }) {
  return (
    <Container navigation={navigation}>
      <Text>Nội dung của Contact...</Text>
    </Container>
  );
}